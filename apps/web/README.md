# Fx Crypto Edge — web

Next.js frontend. See the [repo root README](../../README.md) for full setup instructions
(installing dependencies, starting Postgres, seeding data, and running both apps together).

Quick start once the API server is running on `http://localhost:4000`:

```bash
npm run dev
```
